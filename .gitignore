# Build artifacts — regenerated by `make`
www/game.js
www/game.wasm
www/game.wasm.map
build/

# Python
server/__pycache__/
*.pyc

# Runtime state — regenerated by the server on first boot / every edit
maps/

# Superseded draft — phi.md is the current design doc and IS tracked
roadmap.md

# User's separate project notes — not part of the engine build
tdf.md
.*.swp
*.swp

# Claude Code harness state
.claude/

# Temporary read-only reference tree (separate CAD software) — never
# modified, never committed, will be removed from this directory later
zenith_reference/
